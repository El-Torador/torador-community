import { RemoveUrlPipe } from './remove-url.pipe';

describe('RemoveUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
