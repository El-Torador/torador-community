import { LisibleDatePipe } from './lisible-date.pipe';

describe('LisibleDatePipe', () => {
  it('create an instance', () => {
    const pipe = new LisibleDatePipe();
    expect(pipe).toBeTruthy();
  });
});
